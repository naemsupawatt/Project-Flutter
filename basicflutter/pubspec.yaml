name: basicflutter
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"
  
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_launcher_icons: "^0.9.2"
  flutter_native_splash: ^1.3.1
  scan: ^1.5.0
  google_place: ^0.4.3


flutter_icons:
  android: true
  image_path: "image/logo.png"

dev_dependencies:
  flutter_test:
    sdk: flutter
  #flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/

flutter_native_splash:
  background_image: "assets/frampage.png"